@model GamingStore.WebUI.ViewModels.Address.AddressFormViewModel

@{
    ViewData["Title"] = "Adresi Düzenle";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <partial name="_AddressForm" model="Model" 
                     view-data='new ViewDataDictionary(ViewData) { ["AddressFormStandalone"] = true }' />
        </div>
    </div>
</div>
